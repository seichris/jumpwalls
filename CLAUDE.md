# InfoFi Agent Instructions

Use [`AGENTS.md`](./AGENTS.md) as the single source of truth for:

- InfoFi CLI flow
- required request inputs and outputs
- transaction safety guardrails
- script usage in `scripts-for-ai-agents/`
- domain intent disambiguation (Jump Walls URLs are docs hosts, not default target domains)

If target domain(s) are missing for live-agent listing, ask the user to clarify before proceeding.
